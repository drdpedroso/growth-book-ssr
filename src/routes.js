import Leke from './components/Leke'
import Layout from './components/Layout'

const routes = [{ path: '/', component: Layout, exact: true }, { path: '/leke', component: Leke }]

export default routes
